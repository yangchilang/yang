# 生产环境：使用 Cloudflare Pages Functions 提供的相对路径 /api
# 无需设置 VITE_API_URL，由 Pages Functions 处理后端逻辑

# 解读服务模型名（在 Cloudflare Pages 项目设置中配置为环境变量）
# 推荐 deepseek-v4-flash（最便宜，响应速度也最快）
# VITE_MODEL_NAME=deepseek-v4-flash

# 解读服务密钥（在 Cloudflare Pages 项目设置中配置为环境变量）
# VITE_API_KEY=xxx
