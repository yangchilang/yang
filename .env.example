# 开发环境可选：指定后端 API 地址（如线上 https://tarot-yue.cn 或本地 wrangler pages dev）
# 不设置时默认使用相对路径 /api，由 Cloudflare Pages Functions 提供
# VITE_API_URL=https://tarot-yue.cn

# 解读服务模型名（推荐 deepseek-v4-flash，价格最低，响应速度最快）
# 可选值：
#   deepseek-v4-flash   ← 便宜 / 速度快（推荐，每百万输入约1-3元、输出约2-6元，峰谷不同）
#   deepseek-v4-pro     ← 更强推理（约贵3倍，每百万输入约3-9元、输出约6-27元，峰谷不同）
#   deepseek-chat       ← 旧兼容名（将弃用，目前仍映射到 v4-flash 非思考模式）
#   deepseek-reasoner   ← 旧兼容名（将弃用，映射到 v4-flash 思考模式）
VITE_MODEL_NAME=deepseek-v4-flash

# ===== 密钥安全说明（重要！）=====
# 生产部署（Cloudflare Pages）：
#   前端不会读取任何 VITE_API_KEY。密钥通过 Cloudflare Pages 控制台（或 wrangler secret put）
#   设置到 Functions 环境变量 VITE_API_KEY / DEEPSEEK_API_KEY 或 API_KEY，仅服务端可见。
#
# 本地开发（仅你自己的机器）：
#   有两种方式二选一：
#   1) 推荐：`wrangler pages dev dist`（或 `pnpm run pages:dev`）跑 Functions，
#      此时同源 /api/interpret 可用，不需要任何前端环境变量。
#   2) 便捷：把密钥写到 .env.local 里的 VITE_API_KEY，前端会自动直连 DeepSeek。
#      .env.local 已经被 .gitignore 忽略，不会进仓库。
#
#   无论哪种方式，源码里都没有硬编码任何默认 API Key。
# VITE_API_KEY=
# VITE_API_URL=https://api.deepseek.com/v1/chat/completions
